import 'package:flutter/material.dart';

class ZoneWiseResultPage extends StatelessWidget {
  const ZoneWiseResultPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Zone'),
      ),
    );
  }
}